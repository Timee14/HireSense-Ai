from app.db.session import Base
from app.models.models import (
    User, CandidateProfile, RecruiterProfile, Resume, AIAnalysis,
    Skill, CandidateSkill, Job, JobSkill, Application, MatchScore, Notification
)
